# 手機貪食蛇專案規格書（草案 v0.1）

> 目標：一款「小而美」的手機貪食蛇網頁遊戲，具備震動回饋，
> 並達到接近 Google 官方網頁小遊戲（如搜尋列 `play snake`）等級的操作流暢度。

---

## 1. 背景與目標

### 1.1 目標（Goals）

- 在手機瀏覽器上提供**接近原生 App 的流暢度**（穩定 60 FPS、觸控延遲低於一幀）。
- 提供**震動回饋（Haptics）**，強化吃食物、死亡、升級等關鍵時刻的沉浸感。
- 可安裝為 **PWA**（加到主畫面、離線可玩）。
- 程式碼量小、依賴少，方便長期維護與稽核。

### 1.2 非目標（Non-goals，先明確排除，避免範圍蔓延）

- 不做多人連線/排行榜後端（第一版僅本機最高分）。
- 不支援桌面滑鼠操作為主要輸入（鍵盤可選配支援，但不是設計重點）。
- 不做複雜 3D 效果，維持 2D 極簡美術風格。
- 不客製化震動強度數值介面（沿用預先設計好的震動模式，不開放使用者自訂波形）。
- **不實作 AI 自動駕駛模式**（已決議，見 §8.1），維持純手動操作。

---

## 2. 技術選型與理由

### 2.1 建議技術棧

| 項目 | 選擇 | 理由 |
|---|---|---|
| 語言 | Vanilla JavaScript（ES Modules）+ JSDoc `@ts-check` | 零建置步驟、所見即所得、長期可維護、貼近瀏覽器原生效能，適合小而美的目標 |
| 渲染 | Canvas 2D API | 對「格狀移動 + 少量精靈」的貪食蛇遊戲來說，效能足夠且比 WebGL 簡單很多；DOM 框架反而不適合逐幀重繪場景 |
| 建置工具 | 無（直接用 `npx serve .` 本地測試） | 符合「小」的目標；也可選配 esbuild 做最終上線壓縮（非必要） |
| 型別檢查 | TypeScript compiler（`tsc --checkJs`），不產出 `.js` | 開發期型別安全，執行期仍是純 JS，無編譯落差 |
| PWA | 手寫 Service Worker（`sw.js`）+ `manifest.json` | 完全掌控快取策略，離線後仍可玩 |
| 震動 | `navigator.vibrate()` (Vibration API) | 標準 Web API，無需額外套件；iOS Safari 不支援時需優雅降級 |
| 測試 | Node.js 原生 assert 撰寫的 smoke test（核心邏輯） | 沿用「無框架」哲學，不引入 Jest 等重量測試框架 |
| 音效 | Web Audio API 即時合成（Oscillator/GainNode），無音檔 | 零素材、檔案體積小、可依遊戲狀態動態調整，與 bug-snake 驗證過的做法一致 |
| 多語系 | 手寫 JSON i18n（`i18n/{locale}.json` + `i18n.js`），22 種語系 | 與目前 fork 的 bug-snake 專案語系範圍一致（見 §3.4），沿用相同 key 結構降低維護成本 |

> 💡 **為什麼不用 React/Vue + Vite？**
> 這類框架解決的是「資料驅動 DOM 更新」的問題，但這裡幾乎所有畫面都在 Canvas 裡逐幀重繪，
> 框架的虛擬 DOM diff 機制在此完全用不上，只會增加 bundle 體積與心智負擔，違背「小而美」的目標。

### 2.2 「流暢度」的技術定義（量化標準）

為了確保開發時有明確可驗證的目標，而不是憑感覺講「流暢」：

| 指標 | 目標值 | 測量方式 |
|---|---|---|
| 渲染幀率 | 穩定 60 FPS（裝置支援 120Hz 時可達 120） | Chrome DevTools Performance 面板 |
| 輸入延遲 | 觸控/按鍵到畫面反應 < 1 幀（~16.6ms） | 手動測試 + `performance.now()` 埋點 |
| 遊戲邏輯更新頻率 | 與畫面更新頻率解耦（固定時間步） | 累加器模式（見 §4.2） |
| 首次可玩時間（TTI） | < 1.5 秒（含資源快取後） | Lighthouse |
| 記憶體穩定性 | 長時間遊玩（10 分鐘+）無明顯記憶體成長 | Chrome Memory 面板，檢查物件池是否有效回收 |
| 震動延遲 | 事件觸發到震動 < 50ms | 手動測試（`navigator.vibrate` 呼叫時機埋點） |

---

## 3. 核心遊戲設計（Game Design）

### 3.1 玩法範圍（保持「小」）

- 單一經典無限模式（先不做關卡/時間攻略等多模式，避免第一版範圍過大）。
- 單一食物道具（進階道具留待 v2 規劃）。
- 4 向格狀移動 + 邊界穿牆（wrap-around），無障礙物地圖（v1 先省略迷宮，降低複雜度）。

### 3.2 美術與音效風格：Google 極簡路線

延續 §2.1 的決策，本專案**刻意不走 bug-snake 的「多皮膚/花俏特效」路線**，
而是模仿 Google 搜尋「play snake」內建遊戲的極簡美術語言：

| 面向 | 規格 |
|---|---|
| 色彩 | 單一深色背景（例：`#1b1b1b` 或 `#14181a`），蛇身/強調色採用品牌色 **Jade Cream（Pantone 15-6123 TCX，螢幕近似值 `#9CBBA0`）**，詳見 §8.2 |
| 造型 | 純方塊/圓角方塊組成蛇身與食物，不做像素風、不做漸層貼圖、不做多重皮膚選項 |
| 動畫 | 僅保留「移動插值平滑」與「吃食物時的輕微縮放彈跳」，捨棄粒子特效、螢幕震動等視覺「juice」 |
| 網格線 | 極淡網格線或完全不顯示，維持乾淨畫面 |
| UI 文字 | 使用系統預設無襯線字型（`system-ui`），不額外載入 Web Font，減少首屏等待時間 |
| 音效 | 極簡短音效：吃食物、死亡各一種（各 < 200ms），Web Audio 即時合成，不做動態配樂 |

> 💡 **設計理由**：Google 版好玩的核心在於「上手零門檻、視覺零干擾、專注操作反饋」，
> 不是靠豐富美術取勝。本專案的「精緻感」應該來自**流暢度與反應速度**，而非視覺複雜度。

### 3.3 震動事件對照表

| 事件 | 震動模式（ms） | 觸發時機 | 優先權 |
|---|---|---|---|
| 吃到食物 | `10` | 頭部與食物座標重疊 | 低 |
| 死亡（撞牆/撞自己） | `[30, 20, 60]` | 進入 DYING 狀態瞬間 | 高（可蓋過低優先權事件）|
| 破新高分 | `[15, 15, 15, 15, 40]` | GAME_OVER 時偵測到破紀錄 | 中 |
| 觸控方向鍵按下（可選，預設關） | `5` | D-pad 按下 | 最低（可被使用者關閉）|

**節流規則**：同一 tick 內若多事件同時觸發，僅播放優先權最高者，避免震動堆疊造成廉價感。

### 3.4 多語系範圍

沿用目前 fork 的 `bug-snake` 專案的 22 語系範圍，維持一致的在地化涵蓋率：

```text
zh-TW  en-US  ar-SA  bn-BD  de-DE  es-ES  fa-IR  fr-FR
hi-IN  id-ID  it-IT  ja-JP  ko-KR  nl-NL  pl-PL  pt-BR
ru-RU  th-TH  tr-TR  ur-PK  vi-VN  zh-CN
```

- 以 `en-US.json` 作為 key 結構的參考基準（比照 bug-snake 的 `check-i18n.js` 驗證邏輯）。
- `ar-SA` / `fa-IR` / `ur-PK` 為 RTL（right-to-left）語言，需確保 `<html dir="rtl">` 自動切換與版面鏡像正確。
- 因美術走 Google 極簡風格、文字量少（僅分數、開始/重玩、震動開關等少量字串），語系檔案內容會比 bug-snake 精簡許多，維護成本較低。

### 3.5 統計數據建議（有趣且低成本實作）

在「小而美」原則下，統計數據應該**只存本機（localStorage），不需要後端**，
但透過巧思設計仍可以很有趣、有黏著度：

| 統計項目 | 說明 | 有趣點 |
|---|---|---|
| 🏆 最高分 / 最高長度 | 基本款，bug-snake 已有 | 必備基準線 |
| 🔥 目前連勝場次 / 歷史最佳連勝 | 連續破紀錄視為一次連勝 | 製造「再玩一場就能延續紀錄」的動機 |
| ⏱️ 本場存活時間 vs. 歷史平均 | 死亡後顯示「比平均快/慢 X 秒」 | 即時、可比較的回饋，比單純分數更有感 |
| 🍎 終身累積吃過的食物數 | 跨場次累加 | 營造長期陪伴感，輕量成就系統 |
| 📈 分數成長曲線（近 7 場） | 用小型 sparkline 顯示最近幾場分數趨勢 | 視覺化進步，不需後端也能做（純 Canvas 畫幾條線） |
| 🎯 「差一點」紀錄 | 記錄「距離破紀錄還差幾分」的最接近一次 | 製造惋惜感，誘發再玩一次 |
| 🕹️ 總遊玩場次 / 總遊玩時數 | 累積型數據 | 呼應「終身成就」概念，不需要跟其他玩家比較也有意義 |
| 🐍 平均每次死亡的死法分布 | 例：「撞牆 60% / 撞自己 40%」 | 幽默、自嘲風格的數據，適合分享到社群 |

> 💡 建議第一版先做「最高分／最高長度／終身食物數／總遊玩場次／連勝」五項即可，
> 其餘可留在 v2，避免資料模型一開始就過度複雜。完整分階段規劃見 §8.3。

---

## 4. 架構設計

### 4.1 模組劃分（延續 bug-snake 驗證過的分層模式）

```text
src/
  core/
    grid.js            // 座標系統、邊界/wrap 邏輯
    snake.js           // 蛇身資料結構、移動、碰撞偵測
    food-manager.js     // 食物生成與碰撞檢查
    game-loop.js        // 固定時間步遊戲迴圈
    state-machine.js     // TITLE / PLAYING / DYING / GAME_OVER
    game.js             // Facade，協調以上模組
    config.js           // 常數設定
  render/
    renderer.js         // Canvas 2D 繪圖（Google 極簡風格）
  audio/
    audio-engine.js     // Web Audio 即時合成音效（吃食物/死亡）
  input/
    input-manager.js     // 鍵盤 + 觸控滑動 + D-pad
  i18n/
    i18n.js             // 語系載入與切換邏輯
  utils/
    haptic-manager.js    // 震動回饋（新模組，見 §4.3）
    stats-manager.js     // 統計數據存取（見 §3.5）
  main.js                // 入口，DOM 綁定
i18n/
  en-US.json ... (22 語系檔，結構同 bug-snake，見 §3.4)
  index.json             // 語系清單
sw.js                     // Service Worker
manifest.json             // PWA 設定
index.html
styles/main.css
```

### 4.2 遊戲迴圈：固定時間步 + 插值（流暢度關鍵）

沿用經過驗證的「累加器」模式，讓邏輯更新頻率與畫面刷新頻率脫鉤：

```text
每個 requestAnimationFrame:
  deltaTime = 現在時間 - 上次時間
  accumulator += deltaTime
  while (accumulator >= step) {
    update()          // 固定頻率的邏輯更新（例：8 tick/秒起跳）
    accumulator -= step
  }
  alpha = accumulator / step
  render(alpha)        // 用 alpha 在「前一格」與「目前格」間插值，畫面平滑移動
```

> 這是让蛇「格狀移動」看起來絲滑而非一格格跳動的關鍵技巧，也是 Google 級網頁遊戲常見手法。

### 4.3 HapticManager 設計（新模組，單一職責）

```text
class HapticManager {
  enabled: boolean            // 對應設定開關，存 localStorage
  supported: boolean          // 'vibrate' in navigator 偵測結果

  trigger(eventName)          // 語意化呼叫，例：trigger('eat_food')
  _resolvePattern(eventName)  // 事件名稱 → 震動模式對照表（§3.3）
  _throttle()                 // 同 tick 內防止震動堆疊
}
```

- 呼叫端（`game.js`）只呼叫 `haptic.trigger('eat_food')`，不直接碰 `navigator.vibrate()`，方便未來替換/測試。
- iOS Safari 不支援時，`supported = false`，`trigger()` 直接 no-op，不影響其他邏輯。

### 4.4 觸控輸入的流暢度細節

- 使用 `touchstart`/`touchmove`/`touchend`，並在事件監聽時加上 `{ passive: true }`（除非需要 `preventDefault`），避免捲動事件與遊戲手勢衝突造成的輸入延遲。
- 滑動手勢判斷採用「最小滑動距離門檻」（例：30px），避免手震誤觸發轉向。
- 方向輸入採用「佇列 + 反向過濾」機制（bug-snake 已驗證的模式）：最多緩衝 2 個方向指令，且過濾掉「立即反向」的非法輸入，避免蛇瞬間自撞。

### 4.5 AudioEngine 設計（極簡版）

比照 bug-snake 的 Web Audio 即時合成做法，但範圍大幅縮小：

```text
class AudioEngine {
  init()                 // 建立 AudioContext（需在使用者互動後才能啟動，符合瀏覽器自動播放政策）
  playEat()               // 短促上升音（Oscillator + GainNode envelope，< 150ms）
  playDeath()              // 短促下降音（< 200ms）
  setMuted(bool)           // 對應設定面板靜音開關，存 localStorage
}
```

- 不做動態配樂、不做音高隨長度變化（那是 bug-snake 的「juice」，本專案刻意捨棄以維持極簡）。
- 靜音開關與 §4.3 的震動開關應在同一個設定區塊呈現，UI 越少越好。

### 4.6 StatsManager 設計

對應 §3.5 的統計數據，設計一個獨立模組，避免統計邏輯散落在 `game.js` 各處：

```text
class StatsManager {
  recordGameEnd({ score, length, survivalSeconds, deathReason })
  getSummary()             // 回傳最高分/連勝/終身食物數/平均存活時間等彙總資料
  _load() / _save()         // localStorage 持久化，key 命名沿用 bug-snake 慣例（bugbuster_ 前綴風格）
}
```

- 第一版僅實作 §8.3 v1 清單的五項核心統計（最高分/最高長度/終身食物數/總遊玩場次/連勝），皆為「累加型」數字欄位，其餘（陣列型/比較型數據）留待 v2。

---

## 5. PWA 與離線支援

- `manifest.json`：定義 `display: standalone`、圖示、主題色，讓使用者可加到主畫面，全螢幕沉浸體驗（無瀏覽器網址列，減少視覺干擾）。
- `sw.js`：預快取所有核心資源（HTML/JS/CSS/圖示），確保離線也能玩。
- **重要不變量**：新增任何 `.js` 模組後，必須同步加入 `sw.js` 的快取清單，否則離線模式會壞掉（沿用 bug-snake 專案已驗證過的教訓）。

---

## 6. 驗收標準（Acceptance Criteria）

- [ ] 手機瀏覽器（Android Chrome + iOS Safari）皆可正常遊玩，60 FPS 穩定
- [ ] 吃食物、死亡時，支援震動的裝置有明顯不同節奏的震動回饋
- [ ] iOS Safari（不支援震動）完全靜默降級，無錯誤、無畫面異常
- [ ] 可加到主畫面，離線飛航模式下仍可開啟遊玩
- [ ] 設定面板可關閉震動，且狀態可持久化保存
- [ ] Lighthouse PWA 分數 ≥ 90
- [ ] 觸控滑動操作零誤觸自撞（反向輸入過濾邏輯正確）
- [ ] 音效正常播放且可靜音，靜音狀態可持久化
- [ ] 22 語系檔案通過 `check-i18n` 驗證（key 結構與 `en-US.json` 一致）
- [ ] RTL 語系（`ar-SA`/`fa-IR`/`ur-PK`）版面鏡像正確
- [ ] 第一版統計數據（最高分/最高長度/終身食物數/總遊玩場次/連勝）正確累計並持久化

---

## 7. 開發階段規劃（建議里程碑）

| 階段 | 內容 |
|---|---|
| M1 | 核心邏輯（Grid/Snake/GameLoop/StateMachine）+ 純鍵盤操作可玩版本 |
| M2 | Canvas 渲染（Google 極簡風格 + Jade Cream 品牌色）+ 插值動畫，達成視覺流暢度目標 |
| M3 | 觸控輸入（滑動 + D-pad）+ 行動裝置版面 |
| M4 | HapticManager 震動回饋 + AudioEngine 音效整合 |
| M5 | PWA（manifest + service worker）+ 離線測試 |
| M6 | i18n 22 語系導入 + RTL 驗證 |
| M7 | StatsManager 統計數據功能 |
| M8 | 效能量測與調校（Lighthouse、Performance 面板實測，對照 §2.2 指標）|

---

## 8. 已決議事項（更新自 v0.1 討論）

### 8.1 操作方式：維持純手動操作

- **不實作 AI 自動駕駛模式**（bug-snake 的 `ai-pilot.js` 概念不移植）。
- 理由：AI 尋路（Greedy/BFS/A*）是 bug-snake 用來展示技術深度的「加分項」，
  但對本專案「小而美」的定位是不必要的複雜度，且會增加 `Grid`/`Snake` 之間
  額外的介面設計負擔（AI 需要查詢蛇身佔用、地圖可達性等）。
- 唯一的輸入來源：鍵盤方向鍵/WASD（桌面）、滑動手勢 + D-pad（手機），
  與 §4.4 觸控輸入細節一致，不需額外規劃 AI 相關模組。

### 8.2 品牌色：Jade Cream（Pantone 15-6123 TCX）

- **主色（品牌識別色）**：Jade Cream，柔和的灰綠色調。
  - 建議 Hex 近似值：`#9CBBA0`（Pantone 對應 Hex 會依印刷/螢幕校色系統略有差異，
    正式上線前建議用 Pantone 官方 Connect 工具或設計師提供的螢幕安全色再次核對）。
  - 因此本專案**改為採用 Jade Cream 取代原本規劃的「Google 藍」**作為蛇身/強調色，
    §3.2 美術風格表格中的「蛇身用單一飽和色」欄位需更新（見下方調整）。
- **背景色搭配建議**：維持深色背景（`#1b1b1b` 或更偏冷的 `#14181a`），
  讓柔和的 Jade Cream 在深底上有足夠對比度，同時保留「Google 極簡」的克制感。
- **PWA icon / manifest.json `theme_color`**：使用 Jade Cream 作為 `theme_color`，
  背景色 `background_color` 採深色背景，維持一致的品牌識別。
- **無障礙對比度檢查**：Jade Cream 屬於中低飽和度色彩，正式套用前需用
  WCAG 對比度工具驗證文字/圖示在深色背景上的可讀性（目標至少 AA 等級，對比度 ≥ 4.5:1）。

> 📌 **待辦提醒**：Pantone 色票是印刷色彩系統，螢幕顯示（sRGB）沒有官方保證的
> 一對一轉換值，上面的 Hex 為業界常見近似轉換，正式視覺定稿前建議請設計師
> 用色彩管理工具（如 Adobe Color、Pantone Connect）確認最終螢幕呈現色碼。

### 8.3 統計數據：完整建議與 v1/v2 分階段規劃

延續 §3.5 的 8 項建議，這裡給出更明確的「為什麼這樣分階段」與實作優先順序：

#### v1（第一版，隨 M7 里程碑一起做，成本低、資料模型簡單）

| 優先序 | 統計項目 | 為什麼放 v1 |
|---|---|---|
| 1 | 🏆 最高分 / 最高長度 | 唯一「必要」統計，沒有它連基本的成就感都沒有 |
| 2 | 🍎 終身累積吃過的食物數 | 只需一個累加數字，無需場次記錄，實作成本最低 |
| 3 | 🕹️ 總遊玩場次 | 同上，簡單累加，且是後續其他統計（如平均分數）的基礎分母 |
| 4 | 🔥 目前連勝場次 / 歷史最佳連勝 | 需要記錄「上一場是否破紀錄」，邏輯簡單但能顯著提升重玩動機 |

#### v2（等 v1 上線、有真實使用數據後再評估是否需要）

| 統計項目 | 為什麼延後 | 觸發評估時機 |
|---|---|---|
| ⏱️ 本場存活時間 vs. 歷史平均 | 需要額外記錄「每場存活秒數」的歷史陣列或滾動平均值，資料模型多一層 | 若玩家平均遊玩少於 3 場就流失，可優先做這個以增加「比較感」 |
| 📈 分數成長曲線（近 7 場） | 需要保留最近 N 場的分數陣列，且要多寫一段 Canvas sparkline 繪製邏輯 | 若想強化「長期進步」敘事，可在 v1.1 補上 |
| 🎯 「差一點」破紀錄提示 | 需要判斷「本場分數與歷史最高分的差距」並設計文案分級（差 1~5 分 / 差 6~20 分等） | 若想提升重玩率，可用 A/B 測試驗證是否真的有效再決定要不要做 |
| 🐍 死法分布統計（撞牆 vs. 撞自己） | 需要在 `snake.js` 的 `deathReason` 基礎上額外做分類累加 | 若未來想加入「分享戰績」社群功能，這項數據很適合當分享文案素材 |

#### v2 補充建議：新增一項「本次 v1 未列出」但值得評估的統計

| 統計項目 | 說明 | 為什麼值得考慮 |
|---|---|---|
| 🏅 每日/每週遊玩心情摘要 | 例如「本週最佳一場」「本週平均分數」，在標題畫面用一行文字顯示 | 不需要推播通知也能製造「回訪誘因」，且完全用本機 localStorage 的時間戳記就能算出，符合零後端原則 |

> 💡 **分階段的核心邏輯**：v1 只做「累加型」數據（不需要保存歷史陣列，`StatsManager`
> 資料結構就是幾個數字欄位），v2 才做「陣列型/比較型」數據（需要保存多場歷史紀錄），
> 這樣可以確保第一版 `StatsManager` 的 `localStorage` schema 越簡單越好，之後要擴充
> 陣列型資料時也不會動到既有欄位，向後相容。
